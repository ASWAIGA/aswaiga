<p style="color: green"><%= notice %></p>

<div class= "issue-parte arriba">
  <header class= "issue-cabecera">
    <header>
      <h1>Issues</h1>
    </header>
    <div id= "contenidor">

      <p>
      <div class= "search-container">
        <%= form_tag issues_path, method: :get do %>
          <%= text_field_tag :search, params[:search], placeholder: "subject or reference"%>
          <img src="/img/Icon-search.png" alt="icono" class="search-icon">
          <input type="image" src="/img/Icon-search.png" alt="Submit" class="search-icon">

          <%= select_tag :sort, options_for_select([["Order by STATUS", "status_asc"], ["Order by PRIORITY", "priority_asc"], ["Order by ASSIGNED TO", "assign_to_asc"]]), class:"box2" %>
          <button class="btn-filter2" id="filters-button">
            <span> 🡰 </span>
            <img class="filters-icon" src="/img/Icon-filter.png" alt="submit" style=""width: 20px;"">
          </button>
          <% end %>
      </div>
      </p>

      <span style="display: inline-block; margin-left: 20px;">Tags</span>
      <p>
        <label class= "tag-switch">
            <input type="checkbox" id="mySwitch" onclick="myFunction()" class="checkbox-tags">
            <span class="slider"></span>
        </label>
      </p>


      <%= link_to "+ NEW ISSUE", new_issue_path, class:"btn btn-success" %>
      <p>
        <div>
          <button class="boton-new-bulk">
            <img class="img-new-bulk" src="/img/Icon-new-bulk-white.png"/>
          </button>
        </div>
      </p>
    </div>
  </header>
</div>

<script>
function myFunction() {
  var checkBox = document.getElementById("mySwitch");
  if (checkBox.checked == true){
  } else {
  }
}
</script>

<% if @issues.any? %>
<div id="issues">
  <table class="table table-striped table-borderless">
    <thead>
      <tr>
        <th>TYPE</th>
        <th>SEVERITY</th>
        <th>PRIORITY</th>
        <th>ISSUE</th>
        <th>STATUS</th>
        <th>MODIFIED</th>
        <th>ASSIGN TO</th>
      </tr>
    </thead>
      <% @issues.each do |issue| %>
      <tbody>
        <tr>
          <td><%= issue.tipus %></td>
          <td><%= issue.severity %></td>
          <td><%= issue.priority %></td>
          <td><%= link_to issue.issue, issue%></td>
          <td><%= issue.status %></td>
          <td><%= issue.updated_at.strftime("%d %b %Y") %></td>
          <td><%= issue.assign_to %></td>
        </tr>
      </tbody>
      <% end %>
    <% else %>
      <p> No issues found ☹ </p>
    <% end %>

  </table>
</div>